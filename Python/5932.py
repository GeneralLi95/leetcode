#!/usr/bin/env python3
from typing import List
class Solution:
	def validArrangement(self, pairs: List[List[int]]) -> List[List[int]]:
		result = [[]]
		for i in pairs:
			result.append(i)
			pairs.pop(i)
			for j in pairs
				
		
a = Solution()
b = pairs = [[5,1],[4,5],[11,9],[9,4]]
Solution.validArrangement(a, b)