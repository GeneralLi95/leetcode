#!/usr/bin/env python3

from typing import List
class Solution:
	def minSwaps(self, nums: List[int]) -> int:
		result = 0
		for id, x in enumerate(nums)
		
a = Solution()
b = [0,1,1,1,0,0,1,1,0]

Solution.minSwaps(a, b)